---
# This file is normally not necessary in PROD env (vhere ansible is properly configured)
# just to ensure ansible colections ensured
# usage: 'ansible-galaxy collection install -r collections/requirements.yml' 
collections:
  - name: community.general
  - name: community.libvirt
  - name: community.vmware
  - name: community.postgresql
